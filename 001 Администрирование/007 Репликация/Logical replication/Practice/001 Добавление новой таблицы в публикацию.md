> добавить сюда
> CREATE PUBLICATION flink_cdctest_pub FOR TABLE dbo.constants WITH (publish = 'insert, update, delete, truncate');
> новую таблицу

Чтобы добавить ещё одну таблицу в существующую публикацию, используйте команду `ALTER PUBLICATION`:

## 📝 Добавление новой таблицы в публикацию

```sql
ALTER PUBLICATION flink_cdctest_pub ADD TABLE dbo.имя_новой_таблицы;
```

После этого не забудьте **обновить подписку** на стороне подписчика:
```sql
ALTER SUBSCRIPTION имя_вашей_подписки REFRESH PUBLICATION;
```

## 🔧 Дополнительные операции с публикациями

### 1. **Добавить несколько таблиц сразу**
```sql
ALTER PUBLICATION flink_cdctest_pub 
ADD TABLE dbo.table1, dbo.table2, dbo.table3;
```

### 2. **Удалить таблицу из публикации**
```sql
ALTER PUBLICATION flink_cdctest_pub DROP TABLE dbo.имя_таблицы;
```

### 3. **Проверить, какие таблицы уже в публикации**
```sql
SELECT schemaname, tablename 
FROM pg_publication_tables 
WHERE pubname = 'flink_cdctest_pub';
```

### 4. **Изменить операции для публикации** (если нужно)
```sql
ALTER PUBLICATION flink_cdctest_pub 
SET (publish = 'insert, update');
```

## ⚙️ Полная настройка логической репликации

Если вы добавляете таблицу в уже работающую репликацию:

1. **На издателе** добавьте таблицу:
   ```sql
   ALTER PUBLICATION flink_cdctest_pub ADD TABLE dbo.новая_таблица;
   ```

2. **На подписчике** создайте такую же таблицу (схему нужно скопировать вручную).

3. **Обновите подписку**:
   ```sql
   ALTER SUBSCRIPTION flink_cdctest_sub REFRESH PUBLICATION;
   ```

## ⚠️ Важные замечания

- **DDL изменения не реплицируются** — структуру таблиц нужно копировать вручную.
- **Ограничения REPLICA IDENTITY** — для операций UPDATE/DELETE у таблицы должен быть установлен правильный `REPLICA IDENTITY`.
- **Производительность** — добавление больших таблиц в работающую репликацию может создать нагрузку.

## 💡 Пример полного workflow

```sql
-- 1. Проверим текущие таблицы
SELECT schemaname, tablename FROM pg_publication_tables 
WHERE pubname = 'flink_cdctest_pub';

-- 2. Добавим новую таблицу
ALTER PUBLICATION flink_cdctest_pub ADD TABLE dbo.transactions;

-- 3. Проверим, что добавилось
SELECT schemaname, tablename FROM pg_publication_tables 
WHERE pubname = 'flink_cdctest_pub';

-- 4. Обновим подписку (на подписчике)
-- ALTER SUBSCRIPTION flink_cdctest_sub REFRESH PUBLICATION;
```
