Связь между слотом и публикацией — один из самых важных моментов в логической репликации PostgreSQL. Давайте разберёмся подробно.

## 🔗 Как связаны слот и публикация

**Слот репликации** — это **точка потребления WAL-журнала** для конкретного подключения (подписчика).

**Публикация** — это **список таблиц и операций**, которые можно реплицировать.

**Связь устанавливается при создании подписки**: когда вы создаёте `SUBSCRIPTION`, PostgreSQL:
1. Создаёт слот репликации на издателе (или использует существующий)
2. Начинает отслеживать изменения в таблицах из публикации
3. Записывает изменения в WAL с указанием слота-получателя

## ⚠️ Важное ограничение архитектуры

**Один слот = один поток потребления WAL**. Все изменения из ВСЕХ таблиц публикации проходят через ОДИН слот.

### Неправильный подход (так делать НЕЛЬЗЯ):

```sql
-- НЕ РАБОТАЕТ: Нельзя привязать несколько слотов к одной публикации
CREATE PUBLICATION pub_for_10_tables FOR TABLE t1, t2, t3, ..., t10;

-- Попытка создать 10 подписок на одну публикацию:
CREATE SUBSCRIPTION sub1 
CONNECTION '...' 
PUBLICATION pub_for_10_tables 
WITH (create_slot = true, slot_name = 'slot1');

CREATE SUBSCRIPTION sub2 
CONNECTION '...' 
PUBLICATION pub_for_10_tables 
WITH (create_slot = true, slot_name = 'slot2'); -- ОШИБКА!
```

## 🎯 Правильные способы распараллеливания

### Способ 1: Одна публикация на таблицу + один слот на публикацию

```sql
-- 1. Создаем 10 отдельных публикаций
CREATE PUBLICATION pub_t1 FOR TABLE t1;
CREATE PUBLICATION pub_t2 FOR TABLE t2;
-- ... и так для всех 10 таблиц

-- 2. Создаем 10 подписок (и соответственно 10 слотов)
CREATE SUBSCRIPTION sub_t1 
CONNECTION 'host=... dbname=...' 
PUBLICATION pub_t1 
WITH (create_slot = true, slot_name = 'slot_t1');

CREATE SUBSCRIPTION sub_t2 
CONNECTION 'host=... dbname=...' 
PUBLICATION pub_t2 
WITH (create_slot = true, slot_name = 'slot_t2');
-- ... и так далее
```

### Способ 2: Группировка таблиц по нескольким публикациям

Если 10 слотов слишком много, можно сгруппировать таблицы:

```sql
-- Группируем по 2-3 таблицы на публикацию
CREATE PUBLICATION pub_group1 FOR TABLE t1, t2, t3;
CREATE PUBLICATION pub_group2 FOR TABLE t4, t5, t6;
CREATE PUBLICATION pub_group3 FOR TABLE t7, t8, t9;
CREATE PUBLICATION pub_group4 FOR TABLE t10;

-- Создаем 4 подписки (и 4 слота)
CREATE SUBSCRIPTION sub_group1 ... PUBLICATION pub_group1 ...;
CREATE SUBSCRIPTION sub_group2 ... PUBLICATION pub_group2 ...;
CREATE SUBSCRIPTION sub_group3 ... PUBLICATION pub_group3 ...;
CREATE SUBSCRIPTION sub_group4 ... PUBLICATION pub_group4 ...;
```

### Способ 3: Один слот + фильтрация на стороне потребителя (современный подход)

Используйте одну публикацию, но распределяйте данные на стороне приложения:

```sql
-- 1. Одна публикация со всеми таблицами
CREATE PUBLICATION all_tables_pub FOR ALL TABLES;

-- 2. Одна подписка (один слот)
CREATE SUBSCRIPTION all_data_sub 
CONNECTION 'host=... dbname=staging' 
PUBLICATION all_tables_pub;

-- 3. На промежуточной БД (staging) фильтруем данные
-- и отправляем в 10 разных процессов
```

## 📊 Сравнение подходов

| Подход | Количество слотов | Управление | Риск отставания | Рекомендация |
|--------|-------------------|------------|-----------------|--------------|
| **10 отдельных публикаций** | 10 | Сложное | Низкий (изолировано) | Для критически важных таблиц |
| **Группировка (4 публикации)** | 4 | Умеренное | Средний | Оптимальный баланс |
| **Одна публикация + фильтрация** | 1 | Простое | Высокий (один слот) | Для некритических данных |

## 🚨 Опасности множества слотов (повторюсь, это важно!)

Для 10 слотов потребуется:

```sql
-- Критические настройки в postgresql.conf:
max_replication_slots = 15           # минимум на 5 больше, чем нужно
max_wal_senders = 15                 # столько же или больше
wal_keep_size = 10GB                 # или больше для каждого слота
max_connections = 100+               # слоты + приложение
```

## 💡 Практический пример с 3 таблицами

```sql
-- 1. Отдельные публикации
CREATE PUBLICATION pub_orders FOR TABLE orders;
CREATE PUBLICATION pub_customers FOR TABLE customers;
CREATE PUBLICATION pub_products FOR TABLE products;

-- 2. Отдельные подписки с явными именами слотов
CREATE SUBSCRIPTION sub_orders 
CONNECTION 'host=consumer1 dbname=replica' 
PUBLICATION pub_orders 
WITH (create_slot = true, slot_name = 'slot_orders');

CREATE SUBSCRIPTION sub_customers 
CONNECTION 'host=consumer2 dbname=replica' 
PUBLICATION pub_customers 
WITH (create_slot = true, slot_name = 'slot_customers');

-- 3. Проверка созданных слотов
SELECT slot_name, database, plugin, active 
FROM pg_replication_slots 
WHERE slot_name LIKE 'slot_%';
```

## 🎯 Вывод

**Невозможно** создать несколько слотов для одной публикации. Если нужно распараллелить потребление:
1. **Создайте отдельные публикации** для каждой таблицы или группы таблиц
2. **Создайте отдельные подписки** — каждая создаст свой слот
3. **Или используйте промежуточный брокер** (Kafka) для распределения данных

Для 10 таблиц лучше всего подходит **Способ 1** (10 отдельных публикаций), но только если вы готовы управлять 10 слотами и их мониторингом.
